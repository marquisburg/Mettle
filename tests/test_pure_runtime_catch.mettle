import "std/io";

var ledger: int32 = 0;

@noinline fn record(x: int32) -> int32 {
  ledger = ledger + x;
  return ledger;
}

@test fn ledger_accumulates() {
  assert_eq(record(2), 2);
  assert_eq(record(3), 5);
}

fn main() -> int32 {
  print("ledger {record(1)}");
  return 0;
}
