import "std/io";
import "std/mem";

fn grow() {
  var buf: int32[] = new int32[8];
  buf[0] = 7;
  println("grew {buf[0]}");
  free(buf.data);
}

fn quiet() forbids alloc {
  grow();
}

@test fn the_prover_is_not_trusted() {
  quiet();
}

fn main() -> int32 {
  quiet();
  return 0;
}
